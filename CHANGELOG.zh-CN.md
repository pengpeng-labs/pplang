# 变更记录

[English](CHANGELOG.md)

本文件只记录已经发布的语言定义变更。开发过程由 Git 历史和 issue 追踪。

## 0.3.1 - 2026-08-28

pplang 独立语言定义仓库的首个公开版本。

- 定义必须显式提供类型实参并采用单态化的泛型函数、struct 和 enum。
- 使用普通函数指针参数表达泛型操作要求。
- 为具体类型实例加入 `sizeof[T]()` 和 `alignof[T]()`。
- 稳定严格布尔条件、词法 block 作用域、无符号整数运算、携带长度的 `str`、
  带检查的切片、tuple、穷尽和类型以及 `[N]T` 数组。
- 发布一一对应的英文与简体中文规范、设计原理、标准库合同和技术参考资料。
- 发布一份规范性 EBNF，以及不依赖诊断文字、覆盖接受/拒绝/运行/trap 结果的
  一致性合同。
- 发布标准库源码，并修复 `StrMap` 复制 key 或 value 分配失败时的状态保持。

# 参与 pplang

[English](CONTRIBUTING.md)

pplang 是语言定义仓库。变更首先按语义清晰度和兼容性审阅，其次才考虑某个
实现是否方便。

## 变更分类

- 澄清只解释已有行为，不改变哪些程序有效，也不改变有效程序的可观察结果，
  可以通过补丁版本发布。
- 修正用于使相互矛盾的规范文字或测试与预期发布语义一致，必须明确说明兼容性
  影响。
- 语言变更会新增语法、改变类型规则或改变可观察运行时行为，必须进入新的次
  版本，并提供设计理由。
- 标准库变更在语言版本内独立遵循语义化版本，并且必须记录所有权与失败行为。

## 必须同步的内容

语言行为变更必须在同一个 pull request 中更新：

1. 具有规范效力的英文规范。
2. 章节结构相同的官方简体中文翻译。
3. 语法发生变化时的 EBNF。
4. 正向、负向或运行时一致性用例。
5. 变更对发布可见时的两份 changelog。

编译器实现细节和工具链命令属于各自仓库。诊断文字不得成为语言合同。

## 文档风格

使用精确、中性的技术语言。规范规则说明程序必须做什么，或实现必须拒绝什么。
设计理由说明问题、备选方案、决定和代价，不记录任务状态或开发时间线。发布
文档不使用 emoji。

英文是规范语言，中文文档是官方翻译，并使用相同 heading 结构。示例和标准库
源码注释使用英文，使一份源码树同时适用于两套文档。

## 验证

提交变更前运行：

```bash
node tools/check-repository.mjs
node tools/run-conformance.mjs /path/to/pp
```

所有示例和标准库 `.pp` 文件也必须通过符合规范的编译器。依赖某一编译器崩溃、
诊断句子、中间表示或命令输出的测试不属于可移植一致性测试。

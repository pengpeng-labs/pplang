fn abs(x: int) -> int {
    if (x < 0) {
        return -x;
    }
    return x;
}

fn max(a: int, b: int) -> int {
    if (a > b) {
        return a;
    }
    return b;
}

fn min(a: int, b: int) -> int {
    if (a < b) {
        return a;
    }
    return b;
}

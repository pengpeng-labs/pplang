# pplang 设计原理

[English](design.md)

本文解释 pplang 0.3.1 背后的稳定设计选择，不具有规范效力；程序行为由
[`spec.md`](spec.md) 定义。

## 1. 问题定义

系统编程课程往往只间接连接多个模型：源语言、类型检查器、生成的机器级
代码、显式内存管理、外部接口和 freestanding 执行。工业语言完整展示了这些
问题，但其完整语义和工具链通常太大，难以作为一个整体检查。

pplang 研究一个更窄的问题：怎样用尽可能小而连贯的语言，同时表达普通程序
和 freestanding 系统代码，并让一个读者能够从语法一直追踪到机器行为？

它不是 C、Rust、Zig、Go 或 Ada 的替代品，而是一套具有清晰边界、主动限制
抽象机制的紧凑系统语言设计。

## 2. 设计目标

### 2.1 有界的语言表面

语法、类型规则和 lowering 模型应当能够被整体理解。只有当一个特性能消除
反复出现的偶然复杂度，或建立可复用的安全性质时，才应加入语言。

### 2.2 Freestanding 能力

核心语言不得要求操作系统、allocator、垃圾回收器、unwinder 或动态加载器。
内核、驱动、分配器和 FFI glue 需要裸指针及目标相关能力，因此这些能力必须
保留。

### 2.3 显式资源行为

分配属于标准库和宿主策略。即使类型系统不证明所有权，API 合同也必须明确
复制、借用和生命周期责任。

### 2.4 可预测的源码和编译行为

语言偏好显式类型、显式泛型实参、少量 desugaring 和确定的求值顺序。这些
属性同时有利于人工审阅和生成代码。

### 2.5 与实现解耦

语言合同描述可观察语义，不规定编译器架构或中间表示。实现可以使用 LLVM、
其他 backend、解释器或直接机器码生成。

## 3. 类型系统边界

pplang 使用静态类型、名义 struct 和 enum、结构化数组和 tuple、词法作用域
以及显式转换。它不尝试所有权证明、子类型、高阶种类多态或通用约束求解。

这一边界来自类型理论中的实用区分：积类型、和类型与一阶函数已经提供显著
建模能力，却不要求大型推导或证明系统。类型错误应当能由使用点附近可见的
声明直接解释。

严格布尔条件阻止 C 风格 truthiness 进入控制流。显式整数转换让位宽和符号
解释保持可见。这些规则比新增表面语法更重要。

## 4. 积类型、和类型与控制流

struct 是名义积类型。enum 是名义和类型，每个变体最多携带一个 payload。
`switch` 是 enum 的消去形式；没有最后 `_` arm 时必须穷尽。

该设计借鉴 ML 家族和现代代数数据类型的 tag、payload 与穷尽性，但主动排除
嵌套模式、guard、多 payload 构造器和通用 `match` 表达式。多个 payload 可以
组合进 struct，使模式分析保持一阶。

tuple 主要用于小型返回值和立即解构，不发展成匿名 record、可下标异构容器
或另一套对象模型。

## 5. 显式泛型

泛型函数和类型使用显式类型实参与单态化。设计把常被合并的两个问题分开：

1. 类型参数化，例如 `Vec[int]` 和 `Vec[u8]`。
2. 所需操作，例如比较或哈希。

pplang 通过显式实例化提供第一项，通过普通函数指针参数表达第二项。因此泛型
排序函数接收比较函数，而不是依赖 trait、类型集合或隐式运算符约束。

这一方式受到 Ada 显式泛型合同启发，但将其缩减为 pplang 已有机制。代价是
调用点存在重复、推导机会更少；收益是调用和生成实例都具有可预测形式。

## 6. 字符串与内存

裸指针无法表达长度、所有权、生命周期和可空性。pplang 不试图在类型系统中
解决全部四项，而采用分层边界：

| 需求 | 机制 |
|---|---|
| 已知范围的字节序列 | `{pointer, length}` 形式的 `str` |
| 拥有型可增长字节 | 标准库 `Buf` |
| 同类型可增长值 | 标准库 `Vec[T]` |
| 显式动态分配 | allocator 函数与 `defer` |
| 硬件和 FFI 访问 | 裸 `*T` 与目标能力 |

`str` 是字节视图而不是 NUL 结尾字符串，因此长度为常数时间、允许内嵌零字节，
协议数据也不依赖文本编码。它不拥有数据：长度检查不能证明底层存储仍然存活。

标准库为容器提供所有权约定，但它们仍是 API 合同，不是编译器证明。这是明确
的设计边界，不表示 pplang 具备与 Rust 相同的内存安全性。

## 7. 方法作为语法糖

方法语法不引入 class、方法表、可见性或动态分发。
`value.operation(arg)` 解析为以 receiver 作为第一个参数的普通函数调用。
指针 receiver 的自动取址消除了常见调用噪音，同时保持普通函数 ABI。

这体现了一个原则：语法应暴露已有机制，而不是建立第二套语义系统。

## 8. 标准库边界

只有必须获得语法或编译器知识的操作才进入语言。分配、字节缓冲、vector、
map、字符串算法和数学属于标准库。volatile 内存、端口 I/O、中断和时钟属于
目标能力。

分离这些层以后，宿主程序、freestanding 内核和嵌入式库可以提供不同策略，
而不改变源语言类型系统。

## 9. 被拒绝的方案

### 9.1 所有权与借用检查

静态生命周期证明能提供更强的内存保证，但会主导语言和编译器。0.3.1 选择显式
所有权合同，并接受类型系统无法阻止无效裸指针使用。

### 9.2 Trait、interface 与隐式泛型推导

这些机制能改善大规模抽象，但需要方法查找、一致性规则、约束求解或运行时
表示决策。函数指针和显式类型实参以更小语义表面覆盖了目标规模。

### 9.3 垃圾回收

强制回收器与可预测 freestanding 执行冲突，也会引入大于核心语言的运行时。
应用可以在合适时以库形式提供 arena 或回收器。

### 9.4 源码级 unsafe 与内联汇编 block

没有强制安全子集的 `unsafe` 标记只是装饰。内联汇编会将语言语法与目标约束
绑定。pplang 改为通过类型、builtin 和外部 glue 保持裸能力可见。

### 9.5 通用元编程与宏

编译期执行和宏可以减少重复，却让源码含义依赖另一套执行系统。显式泛型是
0.3.1 唯一的编译期代码生成机制。

## 10. 评估标准

语言设计变更应回答四个问题：

1. 它是否解决真实 pplang 程序中反复出现的问题？
2. 它的类型与运行时行为能否脱离单一编译器实现进行规范？
3. 该行为能否由小型一致性测试覆盖？
4. 新增机制是否小于它消除的重复复杂度？

无法满足这些标准的功能应当进入库、目标模块、工具链或后续语言版本。

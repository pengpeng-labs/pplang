# pplang 0.3.1 语言规范

[English](spec.md)

状态：**0.3.1 稳定版**。本文档是 pplang 0.3.1 的规范性定义。本文是英文规范
的官方中文翻译；两者存在差异时，以英文规范为准。

本文中的“必须”“不得”“应当”和“可以”分别表示要求、禁止、建议和实现可选项。

## 1. 语言模型

pplang 是一门采用静态类型、词法作用域、值语义和显式资源管理的系统编程
语言，不要求垃圾回收器或隐藏运行时。源文件扩展名为 `.pp`。

一个实现只有在接受全部有效 0.3.1 程序、拒绝违反编译期规则的程序，并实现
本文规定的可观察运行时行为时，才符合 0.3.1 版。诊断文字和编译器命令行接口
不属于标准内容。

## 2. 源码与词法结构

源文件采用 UTF-8。关键字和标识符区分大小写。标识符使用 ASCII 形式
`[A-Za-z_][A-Za-z0-9_]*`；非 ASCII 文本可以出现在注释和字符串中。

关键字如下：

```text
as break continue defer else enum extern false fn for if import in let
return static struct switch true while
```

空白用于分隔 token。`//` 开始行注释。`/*` 开始块注释，并由下一个 `*/`
结束；块注释不能嵌套。

十进制整数字面量由一个或多个数字组成。十六进制整数字面量以 `0x` 或 `0X`
开头。浮点字面量包含十进制数字、小数点和至少一个小数位。0.3.1 不提供指数、
数字分隔符、字符字面量或数值后缀语法。

字符串字面量由 `"` 包围，可以包含 UTF-8 文本以及 `\n`、`\t`、`\r`、
`\\`、`\"` 和 `\0` 转义。原始换行和未知转义是编译错误。字符串值是转义
完成后的字节序列；内嵌零字节不会终止 `str`。

## 3. 语法

[`grammar/pp.ebnf`](grammar/pp.ebnf) 是规范性的上下文无关语法。本文规定的
词法限制和语义有效性规则在该语法之外继续生效。

歧义按照第 6 节的运算符优先级解决。赋值是语句而不是表达式。`else` 属于
最近一个尚未匹配 `else` 的 `if`。

## 4. 类型和值

基础类型如下：

| 类型 | 值 |
|---|---|
| `void` | 没有值；只能作为函数返回类型 |
| `bool` | `false` 和 `true` |
| `int` | 32 位二进制补码有符号整数 |
| `u8`、`u16`、`u32`、`u64` | 对应位宽的无符号整数 |
| `float` | IEEE 754 binary64 值 |
| `str` | 非拥有型字节切片 `{pointer, length}` |
| `*T` | 指向 `T` 的裸指针 |

复合类型包括 `[N]T` 数组、`(T1, T2, ...)` tuple、函数指针、具名 struct、
具名 enum 和显式实例化的泛型类型。`N` 必须是非负编译期整数。struct、enum
和泛型实例采用名义类型；数组和 tuple 采用结构类型。

每个值类型都有零值。整数和浮点零、`false`、空指针、空 `str` 以及递归清零
的数组/tuple/struct 分别是对应零值。enum 的零值是声明的第一个变体；若该
变体携带 payload，则 payload 使用对应类型零值。

数组、tuple、struct、enum 和 `str` 在赋值和传参时按值复制。复制 `str` 只
复制指针和长度。语言不追踪所有权或生命周期。

## 5. 声明、初始化和作用域

每个 block 建立词法作用域。内层声明可以遮蔽外层名称，同一作用域中的重名
声明是编译错误。离开 block 后，其中的绑定不可见。

`let name: T = value;` 声明并初始化局部变量。有初始化值时可以省略类型。
类型具有零值时可以省略初始化值，此时绑定获得该零值。两者同时省略是编译
错误。

`static` 声明一个程序级可变值。初始化表达式必须是编译期常量。省略初始化
时使用该类型的零值。

函数参数和返回类型是显式的。省略 `-> T` 等价于 `-> void`。执行到 `void`
函数末尾会正常返回。执行到非 `void` 函数末尾会返回该类型零值；没有零值的
类型必须在所有路径显式返回。

## 6. 表达式、运算符和转换

操作数和函数实参从左到右求值。`&&` 和 `||` 短路。二元运算符均为左结合，
从低到高的优先级如下：

| 层级 | 运算符 |
|---|---|
| 1 | `||` |
| 2 | `&&` |
| 3 | `|` |
| 4 | `^` |
| 5 | `&` |
| 6 | `== != < > <= >= in` |
| 7 | `<< >>` |
| 8 | `+ -` |
| 9 | `* / %` |

一元 `- ! ~ & *` 以及后缀调用、字段、下标、切片和转换的结合强于二元运算。

算术操作数必须具有相同数值类型。无符号算术按类型位宽模 2 回绕。有符号
算术采用二进制补码回绕。整数除零，以及有符号最小值除以 `-1` 会 trap。
无符号除法、余数和顺序比较按无符号解释。移位量必须位于
`[0, bit_width)`，否则 trap。无符号整数右移为逻辑右移，`int` 右移为算术
右移。

浮点算术和有序比较遵循 IEEE 754 binary64。包含 NaN 的有序比较结果均为
false，包括 `!=`。

`if`、`while` 的条件以及 `&&`、`||`、`!` 的操作数必须是 `bool`。整数不会
隐式转换为 `bool`。

赋值、传参和返回要求声明类型一致。当整数字面量可由目标整数类型表示时，
可以直接用于该类型。非字面量整数类型之间、整数与 `float` 之间，以及整数
与指针之间的转换必须使用 `as`。整数收窄保留低位；无符号扩宽执行零扩展；
有符号 `int` 扩宽保持其数学值。不可表示的转换按照这些位规则执行，不饱和。

相等比较适用于 bool、数值和兼容类型的指针。顺序比较只适用于数值。指针
没有顺序关系。`value in array` 从首元素开始执行相等比较；`byte in string`
在完整 `str` 长度内查找字节值。

## 7. 控制流与延迟调用

`if` 和 `while` 使用严格布尔条件。`break` 和 `continue` 只能用于最近的外层
循环。

`range(n)` 只能作为 `for` 的迭代对象。它只求值一次 `n`，产生从零开始、
小于 `n` 的 `int`；非正 `n` 不产生值。`for value in array` 只求值一次数组
表达式，并按递增下标遍历。循环变量拥有独立词法作用域，每次获得元素副本。

执行到 `defer expression;` 时注册该表达式。已注册表达式在当前函数即将返回
前按后进先出顺序各执行一次，无论函数通过显式 return 还是执行到末尾返回。
defer 不在 block 退出、`break` 或 `continue` 时执行；表达式本身在函数退出
时才求值。

## 8. 聚合类型、和类型与泛型

struct 字段按名称初始化。初始化表达式中每个字段最多出现一次；省略字段使用
对应零值。字段声明顺序决定字段顺序；padding 和外部 ABI 布局由目标定义。

tuple 至少包含两个元素，可以返回，并通过 `let (a, b, ...) = value;` 解构。
0.3.1 不提供 tuple 下标或嵌套解构。extern 签名不得包含 tuple。

enum 变体不携带 payload，或只携带一个 payload。构造写作
`Type.Variant(value)` 或 `Type.Variant()`。`switch` 接受 enum，并支持一层
payload 绑定。不使用 `_` 时，每个变体必须且只能出现一次。`_` 最多出现
一次并且必须位于最后。遗漏或重复 arm 是编译错误。

泛型函数、struct 和 enum 在方括号中声明类型参数。每次使用必须提供全部类型
实参。每组不同实参表示一个独立单态化实例。泛型体不会为类型参数隐式提供
算术、比较、转换或条件能力；所需操作必须通过普通函数指针参数传入。0.3.1 不
提供泛型 extern、类型推导、trait、specialization 或约束求解。

`sizeof[T]()` 和 `alignof[T]()` 为具体类型产生编译期 `u64` 值。它们必须
具有一个类型实参且没有值实参。

方法调用 `receiver.name(args...)` 是 `name(receiver, args...)` 的语法糖。
若第一个参数是 `*T`，且 receiver 是可取址的 `T`，编译器会提供
`&receiver`。该语法不引入方法命名空间、动态分发或可见性规则。

## 9. 字符串、数组、指针与 trap

`str` 是在视图有效期间不可变的字节视图。`len(s)` 返回运行时 `u64` 长度。
`s[i]` 返回 `u8`。`s[lo:hi]`、省略边界的形式和 `s[:]` 返回相同存储上的新
视图。字符串下标和切片分别要求 `0 <= i < len` 和
`0 <= lo <= hi <= len`；违反条件会 trap。

`len(array)` 是编译期元素数。0.3.1 不为数组或裸指针下标增加运行时边界检查；
程序必须提供有效下标和仍然存活的存储。指针加减按指向类型大小缩放。解引用
空指针、无效指针、生命周期已结束的指针或失配对齐指针属于无效程序行为。

`str_from_ptr(pointer, length)` 创建视图，不复制数据也不接管所有权。
`str_ptr(value)` 返回其裸字节指针。调用者必须保证视图每次使用时存储仍有效。

trap 会终止当前程序，pplang 代码无法恢复。具体宿主终止机制不属于标准内容。

## 10. 导入、外部函数与系统能力

`import "relative/path.pp";` 导入相对于当前文件解析的 UTF-8 源文件声明。同一
规范路径最多导入一次，因此 import 环在再次遇到已导入文件时终止。文件不存在
是编译错误。0.3.1 不定义包名或依赖解析语义。

`extern fn` 声明宿主提供的函数。基础整数、`float`、`bool` 和指针使用目标
C ABI 表示。`str` 参数只传递字节指针；需要长度时必须另传整数参数。extern
不得返回 `str`，签名不得包含 tuple 或尚未实例化的泛型类型。struct 和 enum
的 FFI 布局由目标定义，应通过显式 glue code 适配。

裸指针、volatile 访问、端口 I/O、中断控制、时钟和原子操作属于编译器目标
或宿主模块提供的系统能力。0.3.1 有意不提供源码级 `unsafe` 或 `asm`。某个
目标提供 builtin，不代表该 builtin 可以移植到其他目标。

## 11. 已定义边界

pplang 0.3.1 不包含垃圾回收、所有权和借用检查、可选指针语法、trait 和
interface、泛型推导、类型集合、specialization、编译期元编程、闭包、异常、
宏、源码级内联汇编，以及旧数组写法 `[T; N]`。

目标指针位宽、对象对齐、聚合 padding、调用约定、可用系统 builtin 和 trap
交付方式由实现定义，实现必须记录这些选择。编译器架构、IR、目标文件格式、
命令行接口、包管理和 workspace 行为不属于语言规范。

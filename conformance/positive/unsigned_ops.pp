fn main() -> int {
    let high: u32 = (0 as u32) - (1 as u32);
    if (high > (1 as u32)) {
        println(1);
    } else {
        println(0);
    }
    println((10 as u32) / (3 as u32));
    println((10 as u32) % (3 as u32));
    return 0;
}
